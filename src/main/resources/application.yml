app:
  datasource:
    jdbc-url: ${JDBC_DATABASE_URL} #jdbc:postgresql://localhost:5432/userDb
    username: ${JDBC_DATABASE_USERNAME}
    password: ${JDBC_DATABASE_PASSWORD}
spring:
  main:
    allow-bean-definition-overriding: false
  mail:
    default-encoding: UTF-8
    host: smtp.gmail.com
    username: dickenstawanda@gmail.com
    password: 18765432
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    protocol: smtp
    test-connection: false
